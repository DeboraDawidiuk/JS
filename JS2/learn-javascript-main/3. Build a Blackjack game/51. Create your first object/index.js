// Create an object that represents an airbnb castle listing.
// It should contain at least one boolean, one string, one number, and one array
// Log out at least two of the keys using the dot notation

let castleListing = {
    bonito: true,
    endereco: "Londres",
    preco: 150.00,
    avaliacao: [1, 2, 3, 4, 5]
}

console.log (castleListing.endereco)
console.log(castleListing.bonito)
console.log(castleListing.preco)
console.log(castleListing.avaliacao[2])


